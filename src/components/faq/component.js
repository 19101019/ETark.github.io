import React, { useEffect } from "react";


const ParentComponent = (props) => {
	useEffect(() => {
	  window.scrollTo(0, 0);
	}, []);

  return (
    <>
		
	  </>
   );
};

export default ParentComponent;
